# Tests

This folder contains automated checks (tests) that verify app behavior.
For the full beginner-friendly guide, read `TESTING.md` in the repo root.

## Example Structure

- tests/
  - services/
    - hrvAnalysis.test.ts
  - components/
    - StatusCard.test.tsx

## Example Test

See `tests/services/hrvAnalysis.test.ts` for a simple example.

## Start Here

- Read `TESTING.md` for what tests are, how to write them, and how to run them.
